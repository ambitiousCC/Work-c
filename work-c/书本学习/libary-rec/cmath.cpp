#include <iostream>
#include <cmath>//introduce

/**
 *  main functions:
 *  sin() cos() tan() asin() acos() atan()
 *  exp(x)   e^x    ||  log(x)  lnx
 *  log10(x) log10(x)   pow(a,b) a^b
 *  sqrt(x)  x^(1/2)
 *
 *  rounding functions:
 *  ceil(x)  up float number
 *  floor(x) down float number
 *
 *  compare functions:
 *  max(a,b)    min(a,b)    abs(x)
 */

using namespace std;

int main()
{
    //cout<<PI<<endl;//no PT
    return 0;
}

